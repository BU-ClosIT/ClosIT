/** Closet Item */
export type ClosetItem = {
  id: string;
  name: string;
  category: string;
  color?: string;
  size?: string;
  brand?: string;
  purchaseDate?: string;
  imageUrl?: string;
  notes?: string;
};
